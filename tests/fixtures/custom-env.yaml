name: custom-memory-suite
version: 1
category: memory
description: custom env for test
setup:
  - inject_conversation: ./fixtures/convo.json
objective:
  - recall_decision
scoring:
  - type: contains_key_facts
    values: ['postgresql', 'march migration']
    weight: 0.7
  - type: llm_rubric
    rubric: include both facts
    weight: 0.3
difficulty:
  level: medium
reset:
  mode: snapshot_restore
  snapshot: fixture-v1
